# Learn Anything - AI Tutorial Generator
# Environment Configuration Example

# =============================================================================
# API KEYS (Required)
# =============================================================================

# Google Gemini API Key (primary)
GEMINI_API_KEY=your_gemini_api_key_here

# Alternative: Google API Key (will be bridged to GEMINI_API_KEY if GEMINI_API_KEY is not set)
GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# LLM MODEL CONFIGURATION
# =============================================================================

# Default Gemini model (recommended for speed and quality balance)
GEMINI_MODEL=gemini/gemini-2.0-flash

# Alternative models (uncomment to use):
# GEMINI_MODEL=gemini-1.5-pro          # Better quality, slower
# GEMINI_MODEL=gemini-1.5-flash        # Balanced option
# GEMINI_MODEL=gemini-2.0-flash-exp    # Experimental features

# LLM Temperature (0.0 = deterministic, 1.0 = creative)
LLM_TEMPERATURE=0.2

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Custom environment file path (if you want to use a different .env location)
# LEARN_ANYTHING_ENV_PATH=/path/to/custom/.env

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================

# Enable verbose logging (uncomment for debugging)
# DEBUG=true

# Custom output directory (defaults to ./outputs)
# OUTPUT_DIR=/custom/output/path

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. Copy this file to .env and fill in your actual API keys
# 2. The system will automatically bridge GOOGLE_API_KEY to GEMINI_API_KEY if needed
# 3. All agents will use the LLM configuration from this file
# 4. Changes to model or temperature apply to all agents globally
# 5. Keep your .env file secure and never commit it to version control

